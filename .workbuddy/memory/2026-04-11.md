# 2026-04-11 工作日志

## 项目: 政府采购数据采集系统

### 完成内容

1. **项目骨架搭建**
   - 创建完整目录结构 (src/, tests/, scripts/, docs/)
   - 配置 pyproject.toml, pytest.ini, ruff.toml, mypy.ini
   - 配置预提交钩子 (.pre-commit-config.yaml)

2. **核心模块实现**
   - HTTP客户端 (fetcher.py): 支持同步/异步、自动重试、配置化UA
   - HTML/JSON解析器 (parser.py): CSS/XPath选择器支持
   - 限流器 (rate_limiter.py): 内存/令牌桶两种实现
   - 存储层 (storage.py): Repository模式，支持内存/SQLAlchemy
   - 调度器 (scheduler.py): APScheduler定时任务

3. **数据模型**
   - Announcement/Pydantic模型，完整的类型注解
   - 枚举类型 (AnnouncementType, Category)
   - 数据验证器和业务逻辑

4. **爬虫实现**
   - AnnouncementCrawler，支持分页抓取、增量抓取、数据去重
   - 工厂函数 create_announcement_crawler()

5. **数据管道**
   - CleaningPipeline: 数据清洗
   - DeduplicationPipeline: 去重
   - EnrichmentPipeline: 数据增强

6. **测试覆盖**
   - 单元测试: models, parser, rate_limiter
   - 集成测试: crawler
   - pytest fixtures配置

7. **CI/CD配置**
   - GitHub Actions工作流: lint, typecheck, test, security

8. **网站API探测**
   - 目标: https://zcpt.szcg.cn/announcement
   - 发现: Nuxt.js SSR应用，axios.browserBaseURL="/group-tendering-website"
   - API端点返回404，需要JavaScript渲染才能发现
   - 创建API_PROBE_REPORT.md记录探测结果

### API配置更新

1. **确认实际API端点**
   - URL: `https://zcpt.szcg.cn/group-tendering-website/officialwebsite/project/page`
   - 参数: `announcementType`, `current`, `size`, `tenderProjectType`, `ext`
   - 响应格式: 分页JSON，`data.records[]` 包含公告列表

2. **更新文件**
   - `src/config/urls.py` - API URL构建函数
   - `src/config/selectors.py` - API字段映射
   - `src/models/announcement.py` - 数据模型适配
   - `src/core/parser.py` - JSON解析器
   - `src/crawlers/announcement.py` - 爬虫实现
   - `src/config/settings.py` - 配置项

3. **关键API字段**
   - `announcementId` -> `id`
   - `announcementName` -> `title`
   - `tenderProjectTypeDesc` -> `category`
   - `releaseTime` -> `publish_date`
   - `releaseEndTime` -> `deadline`

### 待完成

1. **API端点发现** - 已完成
2. **API配置更新** - 已完成
3. **选择器适配** - 已完成
4. **爬虫端到端验证** - 已完成 ✅

### 修复的问题

1. `AsyncFetcher.fetch()` -> `AsyncFetcher.get()` - 方法名错误
2. `httpx.Limits(max_redirects=10)` -> `httpx.Limits(max_connections=100, max_keepalive_connections=20)` - 参数错误
3. `AnnouncementCrawler` 添加 `_ensure_client()` 异步初始化
4. `Announcement` 添加 `announcement_type` 字段
5. `compute_hash()` 改为静态方法 `compute_hash(content)` + 实例方法 `compute_self_hash()`
6. `InMemoryRepository.create/update` 支持 `Announcement` 和 `AnnouncementCreate` 两种输入

### 验证结果

- **测试**: 40 passed in 2.89s
- **爬虫抓取**: 成功抓取3页共60条数据
- **API状态码**: 200

### Git仓库

- 已初始化Git仓库
- 提交: Initial commit (5400518)
- 修复提交: API URL修复 (3e5f2da)

### 启动采集

- 运行命令: `python -m scripts.run_crawler full --max-pages N`
- API URL: `https://zcpt.szcg.cn/group-tendering-website/officialwebsite/project/page`
- 限流: 60 RPM

### 技术要点

- Python 3.11+, 类型注解完整
- ruff + mypy强制代码规范
- 测试覆盖率目标 > 80%
- 团队提升方案: 8周学习路径，每周技术分享